package goorgeous

// take a text value
// walk the text value from place 0
// if we find a marker, store the current marker position if it's candidate
// continue to walk the text looking for closing marker
// if we find the closing marker, create a new node
// if we don't find the closing marker, carry on

func Render() {
}
