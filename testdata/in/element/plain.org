This is plain text.

This has a -- few--endashes.

It also has a --- few mdash---es.

this should be a code element followed by dashes: =code=---

a dot in a code element such as =.git=, =.hg= or =.bash_profile= should still let it be a code element.
